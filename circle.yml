machine:
  java:
    version: oraclejdk8

general:
  artifacts:
    - encrypted-config-value/build/libs
    - encrypted-config-value-bundle/build/libs

test:
  override:
    - ./gradlew build:
        environment:
          TERM: dumb

deployment:
  release:
    tag: /[0-9]+(\.[0-9]+)+/
    commands:
      - ./gradlew -i bintrayUpload

